<?xml version="1.0" encoding="utf-8"?>

<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <com.google.android.gms.maps.MapView
        android:id="@+id/mapView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <Button
        android:visibility="gone"
        android:id="@+id/buttonValidateResources"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="right|top"
        android:text="@string/buttonValidateResources"
        android:padding="10dp"
        android:layout_marginTop="5dp"
        android:paddingRight="10dp"
        android:onClick="validateResources"/>

    <Button
        android:visibility="gone"
        android:id="@+id/buttonCancelResources"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="right|top"
        android:text="@string/buttonCancelResources"
        android:padding="10dp"
        android:layout_marginTop="50dp"
        android:paddingRight="10dp"
        android:onClick="cancelResources"/>

</FrameLayout>